import { PagePlaceholder } from "#/shared/components/layout/PagePlaceholder";

export default function PaymentsPage() {
  return (
    <PagePlaceholder
      description="Track payment records, Stripe status, refunds, and transaction history."
      title="Payments"
    />
  );
}
